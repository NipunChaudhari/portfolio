import { GalleryItem } from "../types/content";

// No photos were included in the uploads, so this stays empty rather than
// filled with stock/placeholder images. The Gallery section renders a
// designed "add your photos" empty state until entries are added here —
// once they are, no component changes are needed.
export const gallery: GalleryItem[] = [];
