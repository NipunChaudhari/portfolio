import { TestimonialItem } from "../types/content";

// No testimonials were in the uploads. Real quotes need the speaker's
// explicit OK to publish — never invented here, even as a placeholder.
// Add entries once you have them and the Testimonials section renders
// them automatically, no component changes needed.
export const testimonials: TestimonialItem[] = [];
