// TODO: replace with your real deployed URL once you've deployed to Vercel
// (either the default *.vercel.app one or your custom domain). Everything
// that needs the site URL — metadata, sitemap.ts, robots.ts, JSON-LD in
// layout.tsx — imports this one constant, so there's only one place to update.
export const SITE_URL = "https://nipun-chaudhari-portfolio.vercel.app";
